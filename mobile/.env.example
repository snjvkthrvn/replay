# Local development — copy to .env (gitignored) and adjust as needed.
EXPO_PUBLIC_API_URL=https://replay-api-ic3h.onrender.com
EXPO_PUBLIC_WS_URL=https://replay-api-ic3h.onrender.com

# For EAS preview/production builds, do NOT rely on this file.
# Set EAS Secrets so they're injected at build time:
#   eas secret:create --scope project --name EXPO_PUBLIC_API_URL --value https://replay-api-ic3h.onrender.com
#   eas secret:create --scope project --name EXPO_PUBLIC_WS_URL --value https://replay-api-ic3h.onrender.com
# These are referenced as "$EXPO_PUBLIC_API_URL" in eas.json env blocks.
